# heimdall
A command-gated Claude Code plugin that intercepts every Write, Edit, and MultiEdit before it executes (a PreToolUse hook) and forces a structured self-challenge against an extensible, file-type-aware checklist. 
